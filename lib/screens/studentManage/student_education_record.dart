import 'package:flutter/material.dart';

class StudentEducationRecordPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('교육 기록 보기')),
      body: Center(
        child: Text('여기에서 교육 기록을 보여줍니다.'),
      ),
    );
  }
}
